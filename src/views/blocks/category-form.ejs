<form class="category-form">
    <select>
        <% categories.forEach((category) => {%>
            <option value="<%= category.slug %>" 
                    <%= category.slug === categoryObj.slug ? 'selected':'' %>>
                <%= category.type %>
            </option>
        <% }); %>
    </select>
    <input type="search" name="" value="" placeholder="Find A Project...">
</form>